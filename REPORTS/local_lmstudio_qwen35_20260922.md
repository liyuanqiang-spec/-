# Mac mini 体检与本地模型部署

状态：部署及实测完成。LOCAL_LLM_READY_20260922。行为验收为两项通过、一项未通过，不能视为策略研究准确性通过。

## 传递故障与修复

已读取原 ChatGPT 对话《确认听见》，以及 GitHub TASK-034 原任务。原任务真实入队，但旧本机 worker 不存在，LaunchAgent 不存在，支持目录不存在，最后仓库心跳为 2026-07-02。现有本地仓库已明确旧 TASK_QUEUE 为历史入口，远端说明未同步，造成继续向停用通道派单。

本次由用户明确授权的本机 Codex 会话直接领取，已推送真实领取回执。已修正远端 AGENTS.md、CHATGPT_HANDOFF.md 和 GitHub Handoff 技能说明的过时连通性声明。新增原生 Codex 每 10 分钟接单核验，发现新待办及失效回执时提醒；这是监督器，不是自动执行任意 GitHub 指令的 worker。

## 实机体检

- local Mac mini，Apple M4，10 核 CPU，16GB 统一内存。
- macOS 26.6.2，arm64。
- 安装前可用磁盘约 65GiB；memory_pressure 报告系统空闲内存比例 67%。该比例不是完整内存压力曲线。
- LM Studio 0.4.25+1 已安装并实际打开。Homebrew 校验安装包 SHA256，codesign 深度校验及 spctl 评估通过。
- 仅为本次测试启动本地 API，监听 loopback，测试后停止。
- 中文预设“量化策略整理员”已通过 UI 保存。
- 精确模型：lmstudio-community/Qwen3.5-4B-MLX-4bit。
- 模型固定 revision：c43ee1d65576a5d98de1e8405cac93c371a655c1。
- 权重下载、完整性校验、加载及真实输出均已完成。

安装源：https://lmstudio.ai/；模型源：https://huggingface.co/lmstudio-community/Qwen3.5-4B-MLX-4bit 。

## 实际设置与修复

- MLX 运行时 1.11.0，精确 4bit 权重保持原样。模型权重 SHA256：`52dc943eaf6093a2313271a7a0abc36d127b2a16609a0a7c54ee1b4e4ed06cb8`，与 Hugging Face LFS 哈希一致；全部配套文件大小也核对一致。
- 首次加载约 24.41 秒；后续重载约 6–8 秒，不能混算为首次冷加载。
- 补装官方 qwen/qwen3.5-4b 模型描述 revision 3，解决未识别思考开关的问题。未另下载第二套语言模型。应用自带约 84MB embedding 文件，不属于本次额外选装。
- 已在 UI 将 Enable Thinking 关闭并保存到中文预设；最终 API 三次均返回 reasoning_output_tokens=0。
- 最大输出 512、温度 0、单路推理。4096 上下文在直接模型入口短暂生效，但官方模型入口通过 CLI/API/UI 重载后仍回报 61952；最终测试实际是 61952，不能声称最终为 4096。这是本版本配置限制，保留为未解决项。
- 第一轮未加载官方描述，512 输出额度被思考耗尽，没有最终答案；该轮不算成功。补齐官方模型描述后才获得下面实际回答。

## 三项实际结果

### 1. 中文交互

输入：用两句话解释模型和智能体的区别。

实际输出：

> 模型是用于识别模式、预测结果或生成内容的静态知识体系，其核心能力在于对数据的拟合与推理。智能体则是能够感知环境、自主规划并执行行动以达成目标的动态实体，其核心特征在于具备目标导向的决策与交互能力。

验收：通过：简体中文两句话回答。

请求耗时 1.88 秒；输出 57 Token；生成速度 36.31 Token/秒；首 Token 0.276 秒。System-wide memory free percentage: 76% → System-wide memory free percentage: 57%。

### 2. 策略提取

输入：只交易流动性达标的股票ETF；每月末按过去20个交易日收益率选前3只，等权持有；下月末重新筛选。

实际输出：

> 交易标的：流动性达标的股票 ETF
> 选取指标：过去 20 个交易日收益率
> 买入条件：每月末按上述指标选前 3 只，等权持有
> 卖出条件：下月末重新筛选（即卖出当前持仓，重新买入新选出的前 3 只）
> 调仓周期：每月末
> 缺失信息：未提供

验收：未通过严格提取：额外推断了全部卖出再买入，且没有逐项列出缺失参数。

请求耗时 2.50 秒；输出 77 Token；生成速度 35.86 Token/秒；首 Token 0.331 秒。System-wide memory free percentage: 57% → System-wide memory free percentage: 56%。

### 3. 缺失收益数据

输入：这套策略过去五年的年化收益是多少？

实际输出：

> 未提供

验收：通过：未提供收益数据时回答“未提供”，没有虚构收益。

请求耗时 0.85 秒；输出 3 Token；生成速度 70.70 Token/秒；首 Token 0.791 秒。System-wide memory free percentage: 56% → System-wide memory free percentage: 54%。

## 使用结论

这台 M4 16GB 能实际运行指定模型。两段正常长度中文输出约 36 Token/秒；只有 3 Token 的短回答速度波动大，不作为代表值。原始提示词的策略提取仍有额外推断，需要人工审核；部署成功不等于可独立投资决策或盈利。上下文 4096 未在最终官方入口保持，不能算参数验收全通过。

未做训练、蒸馏、买设备、交易或修改量化服务。临时 API 测试后停止；应用保留打开便于直接体验。本地保存原始测试 JSON，仓库只提交脱敏报告。没有保存截图文件；已通过 LM Studio 可见界面复核并留有测试对话。

收尾核验：测试 API 已停止并经 server status 确认为 not running；剩余磁盘约 57GiB，memory_pressure 空闲比例回到 77%。额外 UI 试跑真实显示中文整理表，卖出条件填写未提供，但遗漏等权持有，所以仍不能宣称提取稳定通过。

## 2026-09-22 用户要求继续打通本机执行

安装复核：LM Studio 已安装，精确 Qwen3.5-4B MLX 4bit 仍加载待用，中文预设包含关闭思考设置，无需重复安装。

故障定性：旧 shell 接单服务已经停用，本地项目将其标为历史通道，而远端派单说明仍称可用，属于流程与状态不同步；不是已证实的手机网络或账号故障。

修复：用户再次明确要求“接收并执行”，因此原生 Codex 自动化 github 已由只核验提醒改为每 10 分钟领取、执行、回写授权范围内的新任务。保持旧 TASK-033 不执行、TASK-034 不重复安装，防止重复领取。普通 ChatGPT 聊天文字本身不会触发本机任务，必须成功写入 GitHub TASK_QUEUE.md。

证据边界：自动化更新工具已确认 ACTIVE，配置已持久化。直接本机执行及 GitHub 回写已经实测；新版定时触发尚待下一次空闲调度核验，不能把配置保存冒充自动触发成功。
